// The HTTPS address of this app. Notifications only work there, so the old
// http://<IP>:5001 address shows a link to it. Set when the DuckDNS name is created
// (NOTIFICATIONS_SETUP.md, step 1); leave empty until then.
export const SECURE_APP_URL = '';
